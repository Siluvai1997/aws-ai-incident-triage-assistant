# Demo Notes

## Suggested demo flow
1. Open the Streamlit app locally
2. Load sample incident_001.json
3. Run triage in mock mode first
4. Switch to Bedrock mode if AWS credentials and Bedrock access are enabled
5. Show summary, severity, category, component, and runbook steps
6. Explain that the backend can be deployed to Lambda with Terraform
7. Mention that AWS resources were deleted after testing to avoid ongoing cost
